package com.example.wanderlens

import android.app.Application

class WanderLensApp : Application() {
    
    override fun onCreate() {
        super.onCreate()
        // Initialize Timber, Firebase, etc. here later
    }
}
